kind: pipeline
type: docker
name: default

steps:
  - name: install
    image: python:3.11
    commands:
      - pip install -r requirements.txt || true

  - name: test
    image: python:3.11
    commands:
      - python -m pytest -q || python test_app.py

trigger:
  event:
    - push
